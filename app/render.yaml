services:
  - type: web
    name: next-js
    env: node
    plan: free
    buildCommand: yarn; yarn build
    startCommand: yarn start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
